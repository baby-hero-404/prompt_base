# Specs: UI/UX Skill Consolidation

## Modified Requirements

### REQ-M01: Consolidate UI/UX design into `ux-ui-pro-max`
> ❌ Status: Not Started

**Scenario:**
- WHEN `ux-ui-pro-max`'s `SKILL.md` (or a linked reference file) is read after this change
- THEN it must contain the 60-30-10 rule, 8-point grid, and psychology-laws content previously in `frontend-design`
- AND it must contain the "Forbidden AI Defaults" anti-pattern list (bento grids, mesh/aurora gradients, glassmorphism, "fintech blue") previously in `frontend-design`, preserved as a distinct principles/anti-patterns section — not diluted into the style-lookup material
- AND `frontend-design/` must no longer exist as a directory

## Removed Requirements
- REQ-R01: Remove `frontend-design`
